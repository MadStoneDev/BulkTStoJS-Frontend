const MainHero = () => {
  return (
    <section
      className={`flex flex-col justify-center items-center w-full h-96 min-h-max`}
    >
      <div className={`max-w-3xl`}>
        <h1 className={`text-center`}>
          The Best Typescript to Javascript Convertor.
        </h1>
        <p className={`text-center`}>
          Convert your Typescript code to Javascript with ease.
        </p>
      </div>
    </section>
  );
};

export default MainHero;
